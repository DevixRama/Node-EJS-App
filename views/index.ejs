<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Tailwind CSS via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Task Manager</title>
</head>

<body class="bg-gradient-to-r from-purple-600 to-blue-500 text-white min-h-screen flex flex-col items-center py-10">

    <!-- Form Section -->
    <div class="w-full max-w-lg bg-white text-gray-900 p-6 rounded-xl shadow-lg">
        <h1 class="text-3xl font-semibold text-center mb-4">Create a Task or File</h1>
        <form action="/create" method="post">
            <input class="block w-full px-5 py-3 rounded-md bg-gray-100 text-gray-800 placeholder-gray-500 focus:ring-2 focus:ring-blue-500 outline-none"
                type="text" placeholder="Enter Title" name="title">
            <textarea class="block w-full px-5 py-3 mt-4 rounded-md bg-gray-100 text-gray-800 placeholder-gray-500 resize-none focus:ring-2 focus:ring-blue-500 outline-none"
                placeholder="Enter Details" name="detail"></textarea>
            <button class="w-full mt-4 bg-blue-500 text-white px-5 py-3 rounded-md font-medium hover:bg-blue-600 transition-all duration-300">Create Task</button>
        </form>
    </div>

    <!-- Task Collection -->
    <div class=" max-w-4xl mt-10">
        <h2 class="text-2xl font-semibold mb-4 text-center">Your Tasks</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% if (files.length > 0) { %>
                <% files.forEach(function(value){ %>
                <div class="bg-white w-90 text-gray-900 p-5 rounded-xl shadow-md hover:scale-105 transition-all duration-300">
                    <h2 class="text-xl font-bold mb-3"><%= value %></h2>
                    <div class="flex justify-between">
                        <a class="bg-blue-500 px-4 py-2 rounded-md text-white hover:bg-blue-600 transition" href="/file/<%= value %>">More</a>
                        <a class="bg-gray-300 px-4 py-2 rounded-md text-black mx-2 hover:bg-gray-400 transition" href="/edit/<%= value %>">Edit</a>
                        <a class="bg-red-500 px-4 py-2 rounded-md text-white hover:bg-red-600 transition" href="/delete/<%= value %>">Remove</a>
                    </div>
                </div>
                <% }) %>
            <% } else { %>
                <h3 class="text-white text-center col-span-3">Oops! No tasks yet.</h3>
            <% } %>
        </div>
    </div>

</body>

</html>
